# papino-smm — снапшот-бэкап

Это **резервная копия** скилла. Авторитетная (рабочая) копия —
`~/.claude/skills/papino-smm/` (вне git). Снапшот версионируется здесь, чтобы
скилл нельзя было потерять. Правки вносить в `~/.claude/skills/papino-smm/`,
затем обновлять этот снапшот.
